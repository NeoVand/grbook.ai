# Where things stand (morning of 2026-09-13)

## Knowledge vault

- **Chapter notes:** 69 of 94 units have notes on disk; 4 are verified. The rest of the reading continues at two
  agents at a time, then verification, via the hourly resume job (see `knowledge/_meta/build-status.md`).
- **Legacy inventory:** complete. `knowledge/sources/legacy/overview.md` ranks what to carry forward from the old
  project (parallel-transport lab first).
- **Next vault phases** (book profiles, concept registry, concept notes) are scripted in `knowledge/_workflows/` and
  wait for your go-ahead on pace.

## App skeleton (built overnight, not committed)

Run it with `pnpm dev` (the vault index is rebuilt first), then open http://localhost:5173.

| Area | What exists | Verified |
| --- | --- | --- |
| Pages | Landing, library (`/learn`), chapter study pages (`/learn/<book>/<unit>`), search, settings, sign-in | Rendered in the browser (desktop and mobile); KaTeX renders |
| Knowledge index | `scripts/build-vault-index.ts` compiles dossiers into a searchable index; server loader with MiniSearch | Unit tests |
| Accounts and keys | Better Auth tables; OpenAI and ElevenLabs keys stored encrypted (AES-256-GCM) | Encryption unit tests; forms not exercised (no test account was created) |
| Learner memory | Tables for events, concept state, goals, tutor sessions, notes; visits recorded per day | Schema pushed to `local.db` |
| Voice tutor | `/api/live/session` (GPT-Live WebRTC with the stored key), browser client with the documented tool loop, knowledge tools, tutor dock | Type-checked and config unit-tested; **not yet tested with a live key** |
| Narration | `/api/tts` (ElevenLabs with timestamps), word timings, listen button with word highlighting | Alignment unit tests; **not yet tested with a live key** |

Checks: `pnpm check` clean, server unit tests pass, `pnpm build` succeeds with the Vercel adapter.

## Needs your feedback

1. The architecture and its open questions: `docs/architecture.md` (key storage, database host, backend model,
   visual direction, signed-out access).
2. The look of the pages: dark and light themes follow the system; the library is still organised by book until the
   concept registry exists.
3. Whether to spend usage on live-key testing of the tutor and narration next.

## Known gaps

- The tutor's tool-result timing and data-channel permissions follow the docs but need a live session to confirm.
- The runtime index is about 6.6 MB in the server bundle; it should be split or trimmed before production.
- On Vercel, use a remote libsql URL (for example Turso); the local native libsql module is not deployable.
- The template's demo routes under `src/routes/demo/` are still present.
- No labs yet; the first port (parallel transport) is next after feedback.
